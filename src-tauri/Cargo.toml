[package]
name = "claude-profiles-tauri"
version = "0.1.0"
description = "Personae — run multiple isolated Claude accounts (Desktop + Claude Code CLI) at once."
authors = ["Amory Dev <amory.dev@gmail.com>"]
license = "MIT"
repository = "https://github.com/Amorydev/MultipleClaudeProfile"
homepage = "https://claudemux.com"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "claude_profiles_tauri_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
png = "0.17"
